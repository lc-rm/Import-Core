# 引き継ぎ書(次セッション用)v2

作成日: 2026-05-08
更新: 担当者選択方式・3階層維持・UI要件を反映

---

## 次セッションでやること

### メインタスク
**Import Core を「GitHub Private Repository データ共有方式 + 担当者選択UI」に移行する**

詳細仕様は同梱の **`設計書_GitHub共有方式_v2.md`** を参照。

### 開始時の手順

1. このファイルと `設計書_GitHub共有方式_v2.md` を最初に読み込む
2. 村井さんが「事前準備」(GitHub リポジトリ作成、PAT発行)を完了しているか確認
3. 完了していなければ、まず準備を依頼してから実装に入る

---

## 重要な仕様(必ず尊重すること)

### 認証方式
- **個別PAT発行**(社員1人につき1つのPAT)
- 各社員が自分のPATを Import Core に1回入力すれば、以降は自動ログイン
- パスワード認証は**廃止**

### 担当者選択方式
- ログイン後、**「担当者選択画面」**が表示される
- 自分の名前のカードをクリック → 変換ページへ
- ヘッダー右上に常に「担当: ○○」表示、クリックで切替可能
- 取込履歴に **担当者名** が自動記録される

### 担当者の追加・削除
- **「設定」画面(クライアント・履歴ページ)に「担当者管理」セクション**を新規追加
- **管理者のみ操作可能**(一般ユーザーには表示されない)
- 新規担当者の追加、権限変更(管理者⇄一般)、削除ができる

### 権限階層(従来通り)
- **絶対管理者**: 村井さん(コードで `r_murai@link-core.co.jp` を固定)
- **管理者**: 担当者管理可能、絶対管理者は操作不可
- **一般**: クライアント・取込操作のみ

### UI要件
- ログイン画面・担当者選択画面: **可愛いコアラ画像 + あたたかい配色**(既存テーマ踏襲)
- 担当者選択画面はカード型UI
- 既存の `assets/koala-*.png` を流用OK

### データ構造(主要部分)
- **GitHub Private Repository: `Import-Core-Data`**
- `data.json`: クライアント情報、取込履歴
- `users.json`: 担当者一覧、権限情報

### アクセス制御
- `users.json` に登録された担当者のみログイン可能
- 知らないPATで来た人は弾く
- GitHubの Collaborator 設定 + `users.json` の二重チェック

---

## 環境情報

### プロジェクト
- リポジトリ(コード): https://github.com/lc-rm/Import-Core
- リポジトリ(データ): https://github.com/lc-rm/Import-Core-Data ← **次セッション開始までに作成**
- 公開URL: https://lc-rm.github.io/Import-Core/
- ホスティング: GitHub Pages
- 完全クライアントサイド静的サイト

### 絶対管理者
- メアド: `r_murai@link-core.co.jp`
- 氏名: 村井 莉彩
- GitHub ID: `lc-rm`

### 想定ユーザー
- 2〜5人、フルリモート
- 既存登録: `r_murai@link-core.co.jp`(絶対管理者)、`a_mukaemura@link-core.co.jp`(管理者)

---

## 前セッションで完成した・修正した機能(現状運用版)

GitHub方式移行までの「現状運用版」として、以下の修正は **既に GitHub にアップロード済み**(村井さん作業完了):

### アップロード済み ✅
- **CSV 日付バグ修正(全媒体)**: `converter.js`
- **電話番号 市外局番リスト対応**: `converter.js`
- **取込履歴 - 全クライアント横断・最新5件・行ごと削除**: `settings.html` + `auth.js`
- **Indeed 応募者ページ取込ブックマークレット**: 実機動作確認済
- **Import Core 側 Indeed 取込キューUI**: `index.html`、`sources.js`(`indeed_page` source追加)
- **全媒体で「勤務地」を空欄に統一**: `sources.js`
- **assets/indeed_bookmarklet.js**: assets配下に配置済み

### GitHub方式実装後の扱い
- 上記の機能はすべて維持(削除しない)
- データ層(localStorage → GitHub API)だけ書き換える
- UI部分(Indeed取込キュー、勤務地空欄、市外局番処理)はそのまま使える

---

## 前セッションで未解決の問題(GitHub方式実装で解決される)

### 1. 「他のPCのデータが見えない」問題
- 原因: localStorage はブラウザ/PC独立
- → GitHub方式で **完全解決**

### 2. 「setup.html を直接URL叩くと誰でもアカウント作成できる」問題
- 原因: 静的ホスティングなのでURL叩けば誰でもアクセス可能
- → GitHub方式実装時に **`setup.html` を削除**して解決

### 3. 「複数の社員が独立した管理者アカウントを作ってしまった」問題
- 各自のブラウザで勝手にアカウント作成された(localStorage独立による)
- → GitHub方式で users.json による一元管理になり解決

---

## 次セッションでの実装ステップ概要

詳細は設計書を参照。

1. **Phase 1**: GitHub API ラッパーと認証実装(2〜3時間)
2. **Phase 2**: 認証UI改修(login.html / operator-select.html)(2時間)
3. **Phase 3**: 設定画面に「担当者管理」追加(2時間)
4. **Phase 4**: 既存画面のデータ層更新(2時間)
5. **Phase 5**: 動作確認&運用切替(1時間)

合計: **半日〜1日**で実装完了見込み。

---

## 次セッション開始時のオープニング例

```
前セッションの設計を引き継ぎます。以下を読みました:
- 設計書_GitHub共有方式_v2.md
- 引き継ぎ_次セッション用_v2.md
- 事前準備手順.md

GitHub Private Repository方式 + 担当者選択UI への移行を実装します。

実装に入る前に確認させてください:

1. リポジトリ Import-Core-Data は作成済みですか?
2. 初期 users.json と data.json は配置済みですか?
3. 自分用 PAT は発行済みですか?
4. 既存のlocalStorageデータを捨てる(=新運用から開始)で良いですか?
```

---

## 補足: 実装上の注意点

### 既存コードとの統合

GitHub方式実装時に新しく書く `github-store.js` は、**既存の SecureStore と同じインターフェース** にする:

```javascript
// 既存: localStorage暗号化方式
const SecureStore = {
  loadClients(), addClient(), 
  appendHistory(), getKnownKeys(), getAllHistory(),
  deleteHistoryEntry(), ...
};

// 新規: GitHub方式(同じインターフェース)
const SecureStore = {  // 同じ名前で実装するか、新名前 GitHubStore で
  loadClients(), addClient(),
  appendHistory(), getKnownKeys(), getAllHistory(),
  deleteHistoryEntry(), ...
};
```

これで `index.html` `settings.html` の **既存の呼び出し箇所をほぼ書き換えずに済む**。
ただし、すべて非同期(Promise) なのは現状と同じなので、`await` のままでOK。

### Indeed 取込キューUI

現状の `index.html` には Indeed 取込キューUIが既に実装済み。GitHub方式移行後も**そのまま動く**(キューはメモリ管理、CSV出力時だけ GitHub API で履歴記録)。

### 取込履歴の `keys` 配列

重複チェックキー(`e:メアド` `p:電話番号`)は `data.json` の `history.{client}.{source}[].keys` に保存される。これも現状のロジックそのまま流用できる。

---

引き継ぎ以上。
