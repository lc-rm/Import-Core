# GitHub データ共有方式 - 事前準備手順

次セッションでスムーズに実装に入れるよう、以下を村井さんに進めていただきます。

---

## 1. 新リポジトリ作成(2分)

### 手順
1. https://github.com/new にアクセス
2. **Repository name**: `Import-Core-Data`
3. **Description**(任意): `Import Core 用データ共有リポジトリ`
4. **Public/Private**: **Private** を選択(超重要、絶対 Public にしないこと)
5. ✅ **Add a README file** にチェック
6. **Create repository** ボタンをクリック

### 確認
作成後、URL が `https://github.com/lc-rm/Import-Core-Data` になっていればOK。

---

## 2. 初期データファイルを作成(3分)

### users.json を作成

1. リポジトリの「Code」タブ → 「Add file」 → 「Create new file」
2. ファイル名: `users.json`
3. 内容に以下をコピー&ペースト:

```json
{
  "version": 1,
  "users": [
    {
      "email": "r_murai@link-core.co.jp",
      "displayName": "村井 莉彩",
      "role": "admin",
      "addedAt": "2026-05-08T00:00:00Z",
      "addedBy": "system"
    }
  ]
}
```

4. ページ下部の「Commit changes...」→ 「Commit changes」

### data.json を作成

同様に:

1. 「Add file」 → 「Create new file」
2. ファイル名: `data.json`
3. 内容:

```json
{
  "version": 1,
  "lastModifiedAt": "2026-05-08T00:00:00Z",
  "lastModifiedBy": "system",
  "clients": [],
  "history": {}
}
```

4. Commit

---

## 3. 自分用 Personal Access Token を発行(5分)

### 手順

1. GitHub右上のプロフィールアイコン → **Settings**
2. 左サイドバーを下にスクロール → **Developer settings**(一番下)
3. **Personal access tokens** → **Fine-grained tokens** をクリック
4. **Generate new token** ボタンをクリック
5. 以下を入力:

| 項目 | 値 |
|---|---|
| **Token name** | `Import-Core (村井)` |
| **Expiration** | **90 days**(または `Custom` で任意) |
| **Description**(任意) | `Import Core データ共有用` |
| **Repository access** | **Only select repositories** を選択 → `Import-Core-Data` を選択 |

6. **Permissions** → **Repository permissions** で以下を設定:

| Permission | Access |
|---|---|
| **Contents** | **Read and write** |
| **Metadata** | **Read-only**(Repository access を選んだ時点で自動で付くはず) |

他は **No access** のまま。

7. ページ下部の **Generate token** ボタンをクリック
8. 表示されたトークン(`github_pat_xxxxxxxxxxxx...` という長い文字列)を **絶対に安全な場所にコピー保存**
   - ⚠️ **このページを離れると2度と表示できません**
   - 推奨保存先: 1Password、メモ帳の暗号化ファイル、など

---

## 4. 動作確認(任意)

PATが正しく発行できているか確認したい場合、ターミナル(Mac の「ターミナル」アプリ)で:

```bash
curl -H "Authorization: Bearer YOUR_PAT_HERE" \
  https://api.github.com/repos/lc-rm/Import-Core-Data/contents/users.json
```

成功すると JSON が返ってきます。`"message": "Bad credentials"` などのエラーが出たら PAT が間違っているか、スコープ設定を確認してください。

---

## 5. 完了報告

準備が完了したら、次セッションで:

1. リポジトリ `Import-Core-Data` を作成済み
2. `users.json` と `data.json` を初期化済み
3. 自分用 PAT を発行済み(値は手元に保管)

を確認してから、実装に入ります。

PAT は次セッションでコードに直接埋め込みません(各社員が自分で入力するUIになる)。村井さんの PAT は動作確認用に使うだけなので、私(Claude)に教える必要はありません。

---

## トラブルシューティング

### Q: GitHubでフォルダ作るには?
A: 「Create new file」でファイル名に `folder/file.txt` のように `/` を入れるとフォルダができます。今回は不要(`users.json` `data.json` ともにルート配置)。

### Q: Private リポなのに作れない
A: 個人アカウントの GitHub Free プランでも Private リポは無制限で作れます。たまに「Organization」配下で制限がかかる場合あり。Personal アカウントで作成してください。

### Q: PATの「Fine-grained」と「Classic」の違いは?
A: Classic は古い方式で、リポ全体の権限を持つ強力なトークン。Fine-grained は新しい方式で、リポ単位で権限を細かく設定できる。**Fine-grained を使ってください**(セキュリティ的に望ましい)。

---

以上です。所要時間トータル 10〜15分。
